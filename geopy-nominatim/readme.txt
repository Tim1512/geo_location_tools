This just returns a bounding box (lats/long) based on the named location you input on the command line.

Nominatim is changing, so it'll issue a warning on this version of geopy... something about the default user agent.